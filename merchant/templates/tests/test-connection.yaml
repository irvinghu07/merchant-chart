apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "merchant.fullname" . }}-test-connection"
  labels:
    {{- include "merchant.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "merchant.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
